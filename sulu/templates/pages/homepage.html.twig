{% extends "base.html.twig" %}

{% block content %}
    <h1 class="text-4xl font-bold mb-4">{{ content.title }}</h1>

    <div class="prose prose-lg mb-8">
        {{ content.article|raw }}
    </div>

    <article class="space-y-4">
        {% for product in content.products %}
            <div class="border p-4 rounded">
                <h2 class="text-2xl font-semibold mb-2">{{ product.title }}</h2>
                <p class="text-lg mb-2">
                    {{ product.content.content.price }} €
                </p>
                <a href="{{ product.routePath }}" class="text-blue-500 hover:underline">More info</a>
            </div>
        {% endfor %}
    </article>
{% endblock %}
